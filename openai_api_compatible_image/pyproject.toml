[project]
name = "openai-api-compatible-image"
version = "0.1.0"
description = "OpenAI API Compatible Image Generation Tool Plugin for Dify"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.9.0",
    "requests>=2.34.2",
]

[dependency-groups]
dev = []
